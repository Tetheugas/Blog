

<div class="card">
    <div class="card-body">
        <h4>Categorias: </h4>
        {{#each categorias}}


        <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
        {{else}}
        <h4>Não há categorias para listar</h4>
        {{/each}}
    </div>
</div>